# MediaOps Assets

Static project assets used by MediaOps.

## Docker / Unraid icon

The MediaOps container icon should be stored here as:

`mediaops-icon.png`

Keeping the icon on the stable `main` branch allows Docker and Unraid to reference it through GitHub's raw content URL.
